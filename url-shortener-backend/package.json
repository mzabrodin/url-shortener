{
  "name": "url-shortener-backend",
  "private": true,
  "type": "commonjs",
  "workspaces": [
    "shared",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @url-shortener/shared && npm run build -w @url-shortener/gateway && npm run build -w @url-shortener/shorten && npm run build -w @url-shortener/redirect && npm run build -w @url-shortener/analytics && npm run build -w @url-shortener/stats",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "eslint": "10.2.0",
    "eslint-config-prettier": "10.1.8",
    "prettier": "3.8.2",
    "typescript": "6.0.2",
    "typescript-eslint": "8.58.2"
  }
}
